server:
    port: 2001

spring:
    application:
        name: manager-task-service

    cloud:
        nacos:
            discovery:
                server-addr: localhost:8848  # 配置nacos地址
            config:
                server-addr: localhost:8848  # nacos配置中心地址
                file-extension: yaml  # 配置文件格式
                #group: LOCAL_GROUP
                group: TEST_GROUP

        sentinel:
            eager: true  # 取消控制台懒加载
            transport:
                dashboard: localhost:9090  # sentinel dashboard地址
                port: 8719  # sentinel api port
            # sentinel配置持久化
            datasource:
                ds1:
                    nacos:
                        server-addr: localhost:8848
                        dataId: manager-task-service
                        #groupId: DEFAULT_GROUP
                        groupId: TEST_GROUP
                        data-type: json
                        rule-type: flow

# 使用openfeign已整合的Ribbon 设置feign客户端的超时时间
ribbon:
    # 建立连接后，从服务器读取到可用资源所用的时间
    ReadTimeout: 20000
    # 建立连接所用的时间，适用于网络状况正常的情况下，两端连接所用的时间
    ConnectTimeout: 20000
#    eager-load:
#        enabled: true
#        clients: manager-task-service, manager-account-service, manager-storage-service